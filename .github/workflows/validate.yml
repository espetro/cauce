name: validate

on:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: DCO sign-off check
        shell: bash
        run: |
          set -euo pipefail
          base="${{ github.event.before }}"
          head="${{ github.sha }}"
          if [ "$base" = "0000000000000000000000000000000000000000" ]; then
            # Branch or tag creation: check the commits listed in the push event.
            mapfile -t commits < <(jq -r '.commits[].id' "$GITHUB_EVENT_PATH")
          else
            mapfile -t commits < <(git rev-list --reverse "$base..$head")
          fi
          if [ "${#commits[@]}" -eq 0 ]; then
            echo "No commits in range; nothing to check."
            exit 0
          fi
          missing=""
          for c in "${commits[@]}"; do
            if ! git log -1 --format='%(trailers:key=Signed-off-by,valueonly)' "$c" | grep -q .; then
              missing="$missing $c"
            fi
          done
          if [ -n "$missing" ]; then
            echo "Commits missing a Signed-off-by trailer (git commit -s):$missing"
            exit 1
          fi
          echo "All ${#commits[@]} commit(s) in range carry Signed-off-by."

      - uses: jdx/mise-action@v2

      - uses: Swatinem/rust-cache@v2

      - name: validate
        run: mise run validate

      # W4-04: AI evals fast gate — the `smoke`-tagged cases over recorded
      # transcripts + replay cassettes, run with the binary `mise run
      # validate` already built (adds <30 s, no rebuild). Fails the job
      # when the score drops below `[ai].baseline - tolerance`.
      - name: ai evals smoke gate
        run: ./target/debug/cauce eval ai evals/ai/*.jsonl --tag smoke --gate

      - name: upload ai evals report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ai-evals-results
          path: evals/results/
          if-no-files-found: ignore

      # W3-08: LOC/complexity report — publishes numbers in the log,
      # always exits 0 (a signal source, not a gate).
      - name: loc report
        run: mise run loc
