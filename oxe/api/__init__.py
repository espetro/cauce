"""FastAPI route modules. ``oxe/api/searx.py`` is the only canonical, extensible
HTTP surface; ``oxe/api/exa.py`` and ``oxe/api/mcp.py`` are compatibility layers
on top of it. See ``.agents/plans/2026-09-17-v0.5.0-archive-rebuild.md``.
"""
