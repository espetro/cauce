# later/: seams that exist in v3.0 but are not built in it

Each stub names the seam in the v3 tree it plugs into, the trigger that promotes it to a
wave, and what it must not change. None of these have an iteration or an EPIC until
promoted; they are `P3` issues on the project so they are visible, not scheduled.
