# cauce v3: Rust core, modular metasearch with a hybrid cache

**Status:** approved 2026-09-21 after a three-round grill (decisions in section 3 and
`.agents/decisions.md`). Nothing is implemented yet; execution is broken into per-wave
subplans under `v3/` (start at `v3/README.md`).
**Supersedes:** `2026-09-17-v0.5.0-archive-rebuild.md` (v2).
**History:** `legacy` = v1 (PyPI 0.4.0), `v2-legacy` = v2, `main` = v3 orphan history.
**Tracking:** https://github.com/users/espetro/projects/23 (one EPIC per wave, one issue per step).

## 1. Why a third attempt

Two attempts (v1 `legacy`, v2 `main`) produced code that passes its own gates and does not
work end to end. Measured on 2026-09-21 against the real stack (details in the session
findings, summarised here so this plan does not depend on chat history):

| Failure | Root cause | Class |
|---|---|---|
| First search intermittently 502s, AI tool calls fail, page 2 always 502 | Only engine is `ddgs` `duckduckgo`; from this machine it fails 4/5 queries and cannot page. `google` and `mojeek` also 0/3. `bing` 3/3, `yahoo` 3/3, `brave` 2/3 (fastest). | upstream choice, no fallback |
| Continuous scroll spins forever | Sentinel ref never attached; button dispatches state but fetch lives in the observer | wiring |
| History and dashboard permanently empty | `SearchService` never writes `search_log`; nothing writes `clicks`; no click endpoint | wiring |
| Settings dialog blank in dev | Vite proxy lacks `/settings`; load failure swallowed | wiring |
| MCP and Exa adapter unreachable | Modules complete and tested, never mounted in `create_app()` | wiring |
| "cached" badge, no TTL/age anywhere | `/search` exposes no cache metadata; badge is a hardcoded button | missing contract |
| AI "streaming" is one delta after 12s; ungrounded answers cached | Blocking provider call; cache gate is model self-confidence, not sources | design |

The verification stack never touched any of these: e2e ran on `wikipedia-opensearch` and
`force=` fixtures (6 pass, 14 skipped), the UI loop's hard gates measure console errors and
axe, unit tests swap the service for fakes. Modules were verified in isolation; the arrows
between them were never a test subject.

Conclusion carried into v3: **the golden path against a real provider and a deterministic
provider is the first artifact, not the last**, and every mounted surface is asserted by a
test that reads the live router table.

## 2. Goals and non-goals

Goals (from the owner, 2026-09-21):

1. Metasearch tool shared by humans (web UI) and agents (HTTP API, MCP) with a TTL cache, so
   a human's lookup is instantly reusable by an agent and vice versa.
2. A modular, better-architected alternative to SearXNG: tail-tolerant scheduling instead of
   wait-for-slowest, engine health and circuit breakers, pluggable engines that contributors
   can fix without recompiling.
3. Core + API/MCP + web UI usable early, with 2 real providers we heavily use (Bing, Brave HTML)
   plus 1 deterministic provider (`replay`) immune to rate limits so agents iterate fast.
4. Runs locally as an OS service under SearXNG's baseline (target: < 80 MB RSS idle, single
   static binary), and is architected so an enterprise deployment is a config change (store
   backend, egress, auth), not a rewrite.
5. Observability that answers "is this result grounded, when was it fetched, what is its TTL,
   which engines contributed, which cache tier served it".

Non-goals for v3.0:

- A general web crawler or independent index.
- Public multi-tenant instance hardening (anti-bot on our own front door, per-user quotas).
- Feature parity with SearXNG's 70+ engines. We ship 2 native + a bridge and a contract others
  can fill.
- Semantic (vector) cache in the first release. It is a feature-flagged tier with a defined
  seam; it lands after the lexical tiers are proven.

## 3. Decisions locked on 2026-09-21

| Topic | Decision | Rationale |
|---|---|---|
| Language | Rust core (single binary) | Owner chose the slower path; goals 2 and 4 are natural in Rust (tokio deadlines, rusqlite with FTS5 compiled in, rquickjs, rmcp) and awkward in Python. Reverses the 2026-09-15 "don't migrate" record, which assumed a fast v1 ship. |
| Web UI | Server-rendered HTML + HTMX + SSE, embedded in the binary | No codegen pipeline, no type-drift gates, streaming via SSE extension. v2's 11.5k-line policy retrofit is not repeated. |
| License | MPL-2.0 for core crates, Apache-2.0 for `engines/` specs and `sdk/` | File-level copyleft on the core, permissive plugins. DCO sign-off for contributors. |
| Real providers | Bing HTML + Brave HTML (native declarative engines) | Only two that work reliably from a residential IP today; no keys. |
| Bootstrap bridge | `exec` engine wrapping `ddgs` (backend `auto`) via the Python SDK | Real results on day 1 while native scrapers stabilise; also the reference implementation of the exec protocol. |
| Mock provider | `replay` engine: recorded cassettes + synthetic mode with latency/failure injection | Deterministic e2e; lets hedging and breakers be tested offline. |
| Storage | SQLite via bundled `rusqlite` (FTS5 on), behind a `Store` trait | libSQL is now "maintained, new work goes to Turso Database (pre-1.0)"; pg0 spawns a daemon; Qdrant Edge/LanceDB are overkill at 5k-100k rows. Postgres+pgvector is the second `Store` impl for enterprise. |
| Canonical wire shape | cauce's own JSON (SearXNG-compatible fields plus `meta`); MCP `exa_search` tool kept as a frozen alias; **no Exa HTTP route** | Existing agent wiring (`~/SEARCH.md`) uses the MCP tool name only; the HTTP Exa shape had no consumer besides curl. |
| Branching | `main` branched to `v2-legacy`, `main` restarted as an orphan; `legacy` kept | Reuse v1/v2 as reference without mixing them into the new tree. Done 2026-09-21 (remote push of `main` pending branch-protection change). |
| Naming | `cauce`, crates `cauce-*`, binary `cauce` on port 4479 | oxmgr and `search.localhost` keep working. |

Grill outcomes (2026-09-21, three rounds), all locked:

| Topic | Decision |
|---|---|
| Wave 0 scope | Real results from the first binary: `exec` runtime + Python SDK + `ddgs_auto.py` are W0 steps. Replay remains the CI engine. |
| Cache key | Excludes `engines` when unset (default set shares one row); includes it when explicitly pinned. |
| Deadline | Hard deadline 3000 ms, hedge trigger P90 of tier-1 EWMA clamped to [300, 1500] ms, `min_results` 5; all configurable. |
| Politeness/egress | Per-engine token bucket 1 req/s burst 3, one fixed UA per engine (no rotation), `Egress` trait with `Direct` + `StaticProxy`; pools/OHTTP later. |
| Admission | Singleflight on cache key, bounded per-engine queue with wait budget, overflow serves stale else 429 + `Retry-After`; per-client fairness in W3. |
| Observability | `tracing` JSONL (daily files, 7-day retention) always; OTLP traces+metrics opt-in via `tracing-opentelemetry` + `opentelemetry-otlp` behind the non-default `otlp` cargo feature, active when `OTEL_EXPORTER_OTLP_ENDPOINT` is set; owned in-process metrics registry (`cauce-core::metrics`, ~150 LoC) rendered as Prometheus text on `/metrics` (loopback, no auth in v3.0); `request_id` (UUIDv7) on every log line, `meta`, `X-Request-Id`, MCP results; `cauce trace <id>` and `cauce tail`; `audit` table for config, cache deletes, breaker changes, spec loads, AI calls, MCP invocations. **P0.** |
| Metrics set | SearXNG parity (per-engine median/P80/P95 split HTTP vs parse, result count, reliability %, errors by type) plus cache hit rate per tier, stale-served, hedge, deadline-hit, TTFR, requests by client kind, zero-result rate, AI tokens/cost. |
| Evals | Engine relevance evals nightly only (never gate merges); AI evals offline over cassettes + recorded transcripts with a 5-case CI smoke gated on baseline minus tolerance. |
| Test placement | pre-push < 3 min and CI fast gate < 8 min run only network-free checks (fmt, clippy, unit, golden path on replay, engine fixtures, routes/wiring tests, AI smoke); nightly runs live smoke, relevance evals, drift report, budgets. Local defaults adapt to available memory/cores. |
| AI provider | OpenAI protocol first (W4-01..04), Anthropic Messages second (W4-05); Bifrost defaults (`http://localhost:8317/v1`, `${env:BIFROST_API_KEY}`); model picker from `/models`. |
| Config | defaults < TOML < `CAUCE_*` env; `${env:NAME}`, `${env:NAME:-default}`, `${env:NAME:?msg}`, `${file:PATH}`, `$$` escape; templates preserved on save; interpolation on by default, `config.interpolation=false` for enterprise. |
| Late waves | v3.0 = W0..W6: W4 AI, W5 archive, W6 conformance suite. Semantic tier, Postgres impl + auth + deploy docs, per-client fairness, keyed-API engine specs, QuickJS, OTA registry, TS SDK, SearXNG-upstream, sqlite-vec ANN are `v3/later/` stubs. |
| Wikipedia | Shipped tier-3 engine, disabled by default, documented for agent iteration alongside `replay`. |
| Data dirs | XDG: `~/.config/cauce/config.toml`, `~/.local/share/cauce/{cauce.db,logs/,models/}`; `CAUCE_CONFIG_DIR`/`CAUCE_DATA_DIR` override. No migration from v2's `~/.cache/oxe`. |
| Distribution | One binary, runtime modes `cauce serve` (full), `cauce serve --headless` (API+MCP), `cauce mcp` (stdio); cargo features `ui mcp ai archive semantic postgres otlp`, defaults `ui mcp ai` (`otlp` non-default). Zero-effort integrations: `cauce mcp`, `/opensearch.xml`, `cauce search` CLI. Budgets: full < 80 MB, headless < 50 MB, mcp < 40 MB idle. |
| License line | `Copyright (c) 2026 Joaquin Terrasa and cauce contributors`; DCO sign-off enforced in CI. |
| Board | 2-week iterations from 2026-09-22; `Team` Systems (core/store/engines/cli/observability) or Product Builders (server/MCP/UI); EPICs `Scheduled`, steps `For Refinement` until picked up; labels `epic feature bug cosmetic infra documentation`. |

## 4. Architecture

```
                 humans                          agents
            ┌──────────────┐        ┌──────────────┬───────────────┐
            │ web UI       │        │ HTTP JSON    │ MCP           │
            │ HTMX + SSE   │        │ /api/*       │ /mcp (rmcp)   │
            │ (askama tpl) │        │ /metrics     │ stdio         │
            └──────┬───────┘        └──────┬───────┴───────┬───────┘
                   └───────────────┬───────┴───────────────┘
                                   ▼  axum router (one binary, one process)
┌──────────────────────────────────────────────────────────────────────────────┐
│ cauce-core :: SearchPipeline                                                    │
│                                                                              │
│  request ─► normalise ─► CacheLookup (tier 1 exact, tier 2 FTS5, tier 3 vec*) │
│                │ miss / stale                                                 │
│                ▼                                                             │
│           Scheduler ─► EngineSet (health-ranked) ─► hedged fan-out ─► merge   │
│           EWMA latency, circuit breaker per engine, P90 hedge, hard deadline  │
│                │                                                             │
│                ▼                                                             │
│           Persist (cache row + FTS + search_log) ─► respond / stream          │
└──────────────┬──────────────────────────────────┬────────────────────────────┘
               ▼                                  ▼
   ┌───────────────────────┐          ┌──────────────────────────────────────┐
   │ Store trait           │          │ Engine trait                         │
   │  sqlite (v3.0)        │          │  declarative (YAML + CSS selectors)  │
   │  postgres (later)     │          │  exec (JSON over stdio, any language)│
   └───────────────────────┘          │  replay (cassettes / synthetic)      │
                                      │  quickjs (later)                     │
                                      │  searxng-upstream (later, optional)  │
                                      └──────────────────────────────────────┘
   * tier 3 is a `later/` seam (`v3/later/semantic-tier.md`); `Store::get_semantic` and
     `put_embedding` stay in the trait as unimplemented stubs
```

### 4.1 Workspace layout

```
cauce/
├── Cargo.toml                 workspace
├── crates/
│   ├── cauce-core/              MPL-2.0. Domain types, SearchPipeline, Scheduler, cache tiers,
│   │                          Store + Engine traits, merge/RRF, health. No HTTP, no SQL.
│   ├── cauce-store-sqlite/      MPL-2.0. rusqlite impl of Store (bundled, fts5).
│   ├── cauce-engines/           MPL-2.0. Engine runtimes: declarative, exec, replay.
│   ├── cauce-server/            MPL-2.0. axum router, HTMX templates (askama), SSE, MCP (rmcp),
│   │                          Exa MCP alias, static assets (rust-embed).
│   └── cauce-cli/               MPL-2.0. `cauce serve`, `cauce search`, `cauce engine test`,
│                              `cauce cache ...`, `cauce record`.
├── engines/                   Apache-2.0. bing.yaml, brave.yaml, wikipedia.yaml + fixtures/
├── sdk/python/                Apache-2.0. cauce-engine-sdk: exec protocol loop + ddgs reference.
├── sdk/ts/                    Apache-2.0. later.
├── tests/e2e/                 golden path (Rust integration tests booting the binary)
└── .agents/                   plans, decisions, notes, docs (kept)
```

Dependency direction is one way: `cauce-server` -> `cauce-engines`, `cauce-store-sqlite` -> `cauce-core`.
`cauce-core` depends on no I/O crate other than `tokio` and `reqwest` behind the `HttpClient`
trait. This is what makes the enterprise swap a config change.

### 4.2 Core contracts (exact shapes, so wave 0 does not guess)

```rust
// cauce-core
pub struct SearchRequest { q: String, page: u8, lang: Option<String>, time_range: Option<TimeRange>,
                           safesearch: SafeSearch, engines: Option<Vec<EngineId>>, client: ClientKind }
pub struct SearchResult  { url: Url, title: String, snippet: String, engine: EngineId,
                           published: Option<DateTime>, score: f32 }
pub struct SearchMeta    { source: Source /* Cache{tier,age_s,ttl_s} | Network */, engines_used: Vec<EngineReport>,
                           deadline_hit: bool, elapsed_ms: u32, request_id: Uuid }
pub struct SearchResponse{ query: String, results: Vec<SearchResult>, meta: SearchMeta }

#[async_trait] pub trait Engine: Send + Sync {
    fn id(&self) -> EngineId;  fn tier(&self) -> Tier;  fn page_size(&self) -> u8;
    async fn search(&self, req: &SearchRequest, budget: Duration) -> Result<Vec<SearchResult>, EngineError>;
}
pub enum EngineError { RateLimited, Blocked /* captcha */, Timeout, Parse(String), Transport(String), NoResults }

#[async_trait] pub trait Store: Send + Sync {
    async fn get_exact(&self, key: &CacheKey) -> Result<Option<CachedSearch>>;
    async fn get_lexical(&self, q: &str, limit: u8) -> Result<Vec<CachedSearch>>;   // FTS5 over query + titles
    async fn put(&self, key: &CacheKey, resp: &SearchResponse, ttl: Duration) -> Result<()>;
    async fn evict_expired(&self) -> Result<u64>;
    async fn log_search(&self, row: SearchLogRow) -> Result<()>;
    async fn record_click(&self, row: ClickRow) -> Result<()>;
    async fn health(&self) -> Result<Vec<EngineHealthRow>>;  async fn put_health(&self, ..) -> Result<()>;
    // stats/history/cache-admin read methods, see DATA section
}
```

`CacheKey` = sha256 over (normalised `q`, page, lang, time_range, safesearch); `engines`
is hashed only when explicitly pinned (`Some`), sorted and deduped. An unpinned request's
hit is a hit regardless of which engines produced it (the v2 property); a pinned request
never shares entries with the default fan-out. `meta.engines_used` tells the consumer where
it came from.

### 4.3 Engine runtimes

**Declarative (YAML)** covers Bing, Brave, Wikipedia. Schema (v1):

```yaml
id: brave
tier: 1                       # 1 fast/reliable, 2 hedge, 3 specialised
page_size: 10
enabled: true                 # auto-register without a [[engines]] entry; default true
request:
  url: "https://search.brave.com/search?q={q}&offset={page0}"
  headers: { Accept-Language: "{lang}", Authorization: "Bearer ${env:BRAVE_KEY:-}" }
  timeout_ms: 2500
parse:
  kind: html                  # html | json
  results: "div.snippet[data-type=web]"
  fields:
    title:   { css: ".title", text: true }
    url:     { css: "a", attr: href }
    snippet: { css: [".snippet-description", ".desc"], text: true }   # css lists: first match wins
detect:
  blocked:  [ "captcha", "unusual traffic" ]   # body substrings -> EngineError::Blocked
  rate_limited_status: [429, 403]
unwrap_redirect:                             # tried in order; first successful unwrap wins
  - { match: "bing.com/ck/a", param: u, strip: "a1", base64: true }        # query-param shape
  - { match: "r.search.yahoo.com", between: { start: "/RU=", end: "/" } }  # path-segment shape
```

`request.url` and `request.headers` values are `{...}` templates: `{q}` (form-urlencoded
query), `{page}` (1-based), `{page0}`, `{offset}` = `(page-1) * page_size`, `{lang}`
(request lang or `en`), plus `{name+N}`/`{name-N}` arithmetic on the numeric names clamped
at 0 (Bing's `first={offset+1}`); `{{`/`}}` are literal braces. Header values may also carry
`${env:NAME}`/`${env:NAME:-default}`/`${file:PATH}` interpolation resolved through the config
interpolator at spec-compile time, before `{...}` templating, so secrets never sit in the
spec.

For `kind: json`, `results` and each field's `path` are JSONPath (`serde_json_path`). A
`results` path selecting a lone array iterates its elements, so parallel-array APIs work:
OpenSearch's `["q", [titles], [descs], [urls]]` is `results: "$[1]"` with fields like
`url: { path: "$[3][{i}]", root: true }`: `{i}` is the result index and `root: true`
evaluates the path against the document root instead of the result node. Field `regex`
(capture group 1, else whole match) applies in both kinds. `url` is the only required field.

The runtime compiles selectors once, fetches with the shared `HttpClient` (rustls, fixed
per-engine UA, per-engine concurrency cap), parses with `scraper`/`serde_json_path`, and
maps to `SearchResult`. `enabled: false` keeps a spec out of auto-registration: it only
runs through an explicit `[[engines]]` entry or `cauce engine test`. Every spec ships with a
`fixtures/<id>/<query>.html` (or `.json`) and an expected JSON; `cauce engine test
engines/brave.yaml` runs the fixtures offline and `--live` runs a canary query.

**Exec (JSON over stdio)** is the language-agnostic contract, identical in spirit to LSP/MCP:

```json
→ {"v":1,"query":"tail tolerance rust","page":1,"lang":"en","timeout_ms":1500}
← {"v":1,"results":[{"title":"...","url":"...","snippet":"..."}],"error":null}
```

One process per engine, kept warm, restarted on crash, killed at deadline. `sdk/python`
provides `cauce_engine_sdk.run(fn)`, and `engines/ddgs_auto.py` (30 lines) is the reference
engine and the day-1 bridge.

**Replay** loads cassettes recorded by `cauce record --engine bing --query ...` (raw response +
parsed results) and serves them deterministically, keyed by normalised query. Synthetic mode
generates N plausible results from a seed and honours `CAUCE_REPLAY_LATENCY_MS`,
`CAUCE_REPLAY_FAIL_EVERY`, `CAUCE_REPLAY_BLOCKED` so scheduler behaviour (hedge, breaker, deadline)
is testable without the network. This is the engine the golden path and the UI iterate on.

### 4.4 Scheduler (the SearXNG difference)

1. `t=0` cache lookup, tiers in order; a fresh tier-1 hit returns immediately; a stale hit is
   returned with `meta.source=Cache{stale:true}` and triggers a background refresh.
2. `t=0` fan out to all **healthy tier-1** engines (Bing, Brave) with the request budget.
3. `t=P90(tier1)` (EWMA-derived, floor 300 ms): if fewer than `min_results` (default 5) have
   arrived, hedge to healthy tier-2 engines (`ddgs` bridge, later Yahoo/Startpage).
4. Hard deadline (default 2500 ms live, configurable): cancel in-flight futures, merge what
   arrived, set `meta.deadline_hit`.
5. Merge: dedupe by normalised URL (scheme, host lowercase, strip tracking params, trailing
   slash), score by RRF across engines, stable order.
6. Health: per-engine EWMA latency (`alpha=0.3`), failure counter, breaker states
   Closed/Open/HalfOpen. `RateLimited`/`Blocked` open the breaker for 15 min, `Timeout` x3 in
   a row for 5 min. Persisted in `engine_health` so a restart does not hammer a blocked engine.
7. Streaming variant (`/api/search/stream`, and the HTMX page): emit `results` SSE events as
   each engine returns; the client appends below the fold, never reorders visible items;
   a final `meta` event closes the stream.

### 4.5 AI mode (wave 4, deliberately late)

Provider call is streamed (SSE from OpenAI-compatible endpoint), tool loop over
`search_web`/`search_archive`, answer cached only when `sources.len() >= 1` and confidence
above threshold, `answer` responses carry `meta` like searches. Not in the golden path for
v3.0 because it depends on a paid provider and was the least broken part of v2.

## 5. Data model (the DATA.md that was missing)

SQLite, WAL mode, one file. Every table has exactly one writer path; that is asserted by a
test that greps `Store` impl call sites (see section 7).

| Table | Purpose | Written by | Read by |
|---|---|---|---|
| `cache_entries(key PK, query, params_json, payload_json, created_at, expires_at, hits, engines_json)` | tier-1 exact cache | `Pipeline::persist` | tier-1 lookup, cache admin UI/API, stats `cache` panel |
| `cache_fts` (FTS5, external content on `cache_entries`: query, titles, snippets) | tier-2 lexical cache + `search_archive` | trigger on `cache_entries` | tier-2 lookup, MCP `search_archive` |
| `cache_vec(key, embedding BLOB)` (feature `semantic`) | tier-3 semantic (seam only; impl deferred to `v3/later/semantic-tier.md`) | `Pipeline::persist` when enabled | tier-3 lookup |
| `search_log(id, ts, query_hash, query, client, source, tier, latency_ms, result_count, engines_json, deadline_hit, query_raw)` | every search request, cache or network | `Pipeline::respond` (unconditional) | history page, dashboard (per day, hit rate, latency percentiles, client split, zero-result queries) |
| `clicks(id, ts, query_hash, url, title, position, client)` | result click-through | `POST /api/click` (UI beacon) and MCP `record_visit` | history page, `user_history` MCP tool, AI `search_archive` boost |
| `engine_health(engine_id PK, ewma_ms, failures, breaker_state, breaker_until, last_ok_at, last_error)` | scheduler state across restarts | `Scheduler` | scheduler, `/api/engines`, engines page |
| `audit(id, ts, actor, action, target, details_json, request_id)` | attributed admin/AI/MCP events | `Store::audit()` from the one place each action happens | `/api/audit`, audit page, `cauce trace` |
| `answers(key PK, query, model, payload_json, sources_json, created_at, expires_at)` | AI answer cache (wave 4) | `answer` route | `answer` route, dashboard |
| `pages(url PK, fetched_at, title, markdown, ...)` + `pages_fts` | archived reader pages (wave 5) | `fetch_and_index` | `search_archive` |

Rules:

- `search_log` is written for **every** request including cache hits, so "hit rate" is
  `count(source=cache)/count(*)`, not `unexpired/rows` (the v2 dashboard's mistake).
- History and dashboard read the same plane (`search_log` joined to `clicks`), fixing the
  "two unsynced data planes" retro item.
- TTL: default 1 h, per-request override capped at 24 h, `evict_expired` every 5 min on a
  tokio interval; `DELETE ... WHERE expires_at < now` on the B-tree index.
- Postgres impl mirrors this schema with `tsvector` replacing FTS5 and `pgvector` for tier 3.

## 6. Wire surfaces

| Route | Purpose | Notes |
|---|---|---|
| `GET /api/search?q&page&lang&time_range&safesearch&engines` | canonical JSON | `SearchResponse` incl. `meta` |
| `GET /api/search/stream` | SSE: `results`, `meta`, `error` events | used by HTMX page and agents that want first results early |
| `GET /api/history?since&q&limit`, `POST /api/click` | search log + clicks | W0 |
| `GET /api/cache`, `GET /api/cache/{key}`, `DELETE /api/cache/{key}`, `DELETE /api/cache?expired|all` | cache management | audited; W0 |
| `GET /api/stats?days` | dashboard aggregates incl. engine table and admission/cache metrics | W0, extended W1-09 |
| `GET /api/audit` | audit rows | W0 |
| `GET /api/engines`, `POST /api/engines/{id}/reset` | health, breaker reset | W1-06 |
| `POST /api/engines/{id}/enable`, `POST /api/engines/{id}/disable` | enable/disable, writes config | audited `engine.enable`/`engine.disable`; W2-05 |
| `GET /metrics` | Prometheus text (loopback) | W1-09 |
| `GET /health`, `GET /api/config`, `PUT /api/config` | liveness, settings (templates preserved) | W0 |
| `POST /api/answer` (SSE) | AI answer frames | W4 |
| `POST /api/pages`, `GET /api/pages/{url}`, `GET /api/archive?q=` | archive | W5 |
| `/mcp` (streamable HTTP) and `cauce mcp` (stdio) | tools: `search_web`, `cache_status`, `cache_invalidate`, `exa_search` (frozen alias) in W1; `search_archive`, `fetch_and_index` in W5 | rmcp |
| `/`, `/search`, `/history`, `/dashboard`, `/cache`, `/engines`, `/audit`, `/trace/{id}`, `/settings`, `/answer`, `/archive`, `/opensearch.xml` | HTMX pages | same handlers, `Accept: text/html`; `requires: ui` |
| `GET /favicon.ico` | embedded SVG site icon | W2 fix (#87); `requires: ui` |

There is deliberately **no Exa-shaped HTTP route**: the only Exa consumer is the MCP tool name.

Every surface above appears in a `ROUTES` table in `cauce-server` (method, path, kind, wave,
`requires` feature, `auth` level) that a test compares against the live axum router and
against this section; a route in one and not the others fails CI. That is the mechanical fix
for "written but never mounted". `cauce serve --headless` mounts only rows without
`requires: ui`.

### 6.1 Observability contract

- Every inbound request (HTTP, MCP, CLI) gets a UUIDv7 `request_id`, carried as a `tracing`
  span field so every event has it, returned in `meta.request_id`, `X-Request-Id`, and MCP
  tool results.
- Events: JSONL under `$CAUCE_DATA_DIR/logs/cauce-YYYY-MM-DD.jsonl` (rotation daily, retention
  7 days default); OTLP traces and metrics when `OTEL_EXPORTER_OTLP_ENDPOINT` is set
  (requires the non-default `otlp` feature).
- Metrics: one owned in-process registry (`cauce-core::metrics`) rendered as Prometheus text
  on `/metrics`, the same numbers rendered on
  `/api/stats` and the pages. Names are listed in `v3/wave-1-engines-and-agents.md` W1-09.
- Audit: `audit` table (`ts, actor, action, target, details_json, request_id`) for config
  changes, cache deletes, breaker transitions and resets, spec loads, AI provider calls, MCP
  tool invocations. Actor = `ui | api | mcp:<client> | cli` plus optional `X-Actor`.
- Tools: `cauce trace <request_id>` (timeline from JSONL), `cauce tail` (pretty follow),
  `/trace/{id}` page.

### 6.2 Admission contract

Singleflight on `CacheKey`; per-engine token bucket (1 req/s, burst 3) feeding a bounded
queue with `admission.max_wait_ms`; on overflow serve a stale row if one exists
(`Source::Cache{stale:true}`, background refresh) else HTTP 429 with `Retry-After` / MCP
`rate_limited{retry_after_s}`; per-client round-robin fairness is deferred to
`v3/later/per-client-fairness.md`.

## 7. Verification policy (the part v1 and v2 did not have)

1. **Golden path, wave 0, always green.** An integration test builds the binary, starts it
   with `CAUCE_ENGINES=replay` and a temp DB, then in one flow: `GET /api/search` (network) ->
   same query again (`meta.source=Cache`, `age_s > 0`) -> `GET /api/history` has 2 rows ->
   `GET /api/stats` hit rate is 0.5 -> `POST /api/click` -> history shows the click -> MCP
   `search_web` via rmcp client returns the cached result with `meta` -> `exa_search` returns
   the same URLs in the frozen Exa shape (W1) -> `DELETE /api/cache/{key}` -> search is network again. Plus the
   HTMX `/search?q=` page contains the result titles and the `cached` badge text with age.
2. **Replay first, live gated.** Everything in CI runs on `replay`. `CAUCE_LIVE=1 cargo test
   --test live` runs Bing + Brave canaries and records fixture drift; it runs nightly and on
   demand, never blocks merges.
3. **Engine fixtures.** Each YAML ships recorded HTML + expected JSON; `cauce engine test` runs
   them in CI. A selector change without a fixture update fails.
4. **Wiring matrix tests.** (a) routes table vs live router vs this document, (b) every `Store`
   write method has exactly one call site in the pipeline (grep-based test, like v2's
   `test_agents_rules.py`), (c) every `Engine` impl is constructible from config.
5. **Resource budget test.** Release binary starts, serves 100 replay searches, RSS stays
   under 80 MB and binary under 30 MB (measured, thresholds tunable in one file).
6. **No fixture-only screens.** A UI template may not ship a state the golden path or an
   engine fixture cannot reach. `force=` style fixtures are gone; the `replay` synthetic mode
   provides empty/error/slow states.
7. Gates: `cargo fmt --check`, `clippy -D warnings`, `cargo test` (incl. golden path), `cargo
   deny` (licenses: MPL-2.0 core must not pull GPL), `cauce engine test`, size/RSS budget. One
   `just validate` / `mise run validate`; CI runs it on every push; pre-push hook runs it.

## 8. Waves

Waves are specified step by step in `v3/` (start at `v3/README.md`, which has the dependency
graph and the pickup procedure). One EPIC issue per wave, one issue per step, 2-week
iterations from 2026-09-22. Each wave ends with the golden path green and the binary swapped
into oxmgr only when it is at least as usable as the previous one.

| Wave | Subplan | Steps | Exit |
|---|---|---|---|
| W0 skeleton, real results, traceable requests | `v3/wave-0-skeleton.md` | 12 | golden path on replay; `ddgs` exec bridge returns real results; `cauce trace` works |
| W1 real engines and agent surfaces | `v3/wave-1-engines-and-agents.md` | 13 | Bing + Brave native; MCP HTTP + stdio; `/metrics`; loopback guard; cutover of `~/SEARCH.md` |
| W2 usable UI and observability pages | `v3/wave-2-ui-and-observability.md` | 11 | all pages; SSE streaming; owner usage week |
| W3 tail tolerance | `v3/wave-3-tail-tolerance.md` | 6 | hedging, SWR, nightly relevance evals |
| W4 AI mode | `v3/wave-4-ai-mode.md` | 5 | streamed grounded answers; OpenAI then Anthropic; AI evals |
| W5 archive | `v3/wave-5-archive-and-semantic.md` | 3 | `fetch_and_index`, `search_archive` |
| W6 store conformance | `v3/wave-6-postgres-and-multi-instance.md` | 1 | conformance suite green on SQLite, parameterised over a connection URL |
| later | `v3/later/*.md` | stubs | semantic tier, Postgres impl + auth + deploy docs, per-client fairness, keyed-API engines, QuickJS, OTA registry, TS SDK, SearXNG upstream, sqlite-vec ANN, release checklist, SearXNG format=json compat shim, engine-param forwarding |

## 9. Resource and latency budget (measured in CI from wave 0)

| Metric | Target | How measured |
|---|---|---|
| Idle RSS `cauce serve` (full) | < 80 MB (SearXNG: 250-600 MB) | budget test, release build |
| Idle RSS `cauce serve --headless` / `cauce mcp` | < 50 MB / < 40 MB | budget test |
| Idle RSS with `semantic` model loaded | < 80 MB on the owner's 8 GB machine | nightly budget, `semantic` on |
| Release binary | < 30 MB | budget test |
| Tier-1 cache hit p50 | < 5 ms | golden path timing |
| First live result (Brave) p50 | < 600 ms | nightly live |
| Full live response p90 | < deadline (2500 ms) | nightly live |

## 10. Crates (versions pinned at wave 0; all checked to exist and be maintained on 2026-09-21)

axum, tokio, tower-http, reqwest (rustls), rusqlite (`bundled`, `fts5`), scraper, serde,
serde_json, serde_norway, askama, rust-embed, rmcp 3.x (`transport-streamable-http-server`,
`transport-io`), tracing + tracing-subscriber, clap, url, uuid, sha2, chrono, proptest,
thiserror, anyhow (bin only), cargo-deny. Later: rquickjs, fastembed, sqlite-vec, sqlx (postgres).

## 11. Open items

1. Owner performs the W1-11 cutover on their machine (oxmgr entry, `~/SEARCH.md`).
2. Owner's own v2 manual-test server (`uvicorn ... --port 4577`, `OXE_BACKENDS=wikipedia-opensearch`,
   started 2026-09-21) is still running from the deleted `.venv`; kill it when convenient.

Resolved 2026-09-21: `origin/main` force-pushed to the v3 orphan (`9f21950`); v2 leftovers
removed (`main-run`, two `worktree-agent-*` branches and worktrees; the six unmerged commits
of the second one are preserved as tag `v2-agent-history-dashboard`); board populated
(EPICs #1-#8, steps #9-#63, later #64-#69).
