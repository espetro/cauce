# 2026-09-22 — W0-08 SearchPipeline notes

- Cyclic dev-deps (`oxe-core` dev-depending on `oxe-engines` for `Replay`) work, but
  only in `tests/` integration tests: unit `mod tests` compile the crate as a distinct
  cfg(test) instance, so `dyn Engine` never unifies ("multiple different versions of
  crate oxe_core"). Shared stubs live in `tests/support/mod.rs`.
- `tracing` callsite interest caches process-wide: a test exercising pipeline spans
  under a captured subscriber must be its own `tests/*.rs` binary, or a sibling test
  evaluating the same callsites under no subscriber poisons them to `never`.
- `Source::Cache.ttl_s` is implemented as *remaining* TTL (W0-08 contract note);
  `response.rs` doc comment still says "TTL the entry was written with" — settled file,
  left as-is.
