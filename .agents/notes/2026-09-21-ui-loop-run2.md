# UI loop run 2 (2026-09-21)

Scope: cheap fixes from run 1 (title underline, hero anchor, header chrome, dashboard cache panels) plus the submit affordance.
Generator: Sonnet 5. Evaluator: fresh isolated Opus subagent per iteration, screenshots plus evidence only.

Hard gates: 27/27 green on both iterations (console 0, warnings 0, 5xx 0, axe critical/serious 0, no layout collapse). e2e: 6 passed, 14 skipped (unbuilt checkpoints).

## Ledger

| iteration | verdict | closed | opened |
| --- | --- | --- | --- |
| 1 | FAIL | none | 1 P0, 2 P1, 3 P2 (six findings) |
| 2 | PASS | submit opacity (P1) fixed; five findings moved to `search.md` Queued improvements | 7 P2 |

Iteration 1 findings:
- P0 result snippet and cached page text preview: queued, needs backend snippets (Wikipedia OpenSearch returns none).
- P1 cache age in the `cached` badge: queued, needs an age field in `/search` JSON.
- P1 submit arrow not dimmed while the input is empty: fixed in `SearchBox.tsx` (40% opacity via `placeholder-shown`).
- P2 glyph on zero-result and empty-source lines, and the `confidence` readout: queued, checkpoint 14 asserts the readout.
- P2 mobile header stacking below 700px: queued.

No finding reappeared after being marked fixed.

## Open P2 findings from iteration 2 (not fixed, not part of the PASS bar)

- Active mode segment is a neutral filled pill, spec says raised white pill (or update `landing.md` and `tokens.md`).
- Result title color ships as daisyUI primary; `search.md` and `patterns-result-list.md` still pin `#1a0dab`.
- Landing pill gutter at 390px is 16px, spec says `100vw - 48px`.
- `ask AI instead` is plain text while `retry` and `view Search` are buttons.
- Empty-sources sentence wraps with a dangling "or" before `view Search`.
- Mid-stream failure state has no `answer` header, empty-sources state does.
- History empty stats line keeps the `(oldest)` suffix after the placeholder.

## Queued and not attempted

Result snippet and preview, cache age, empty-state glyph and confidence decision, mobile toggle stacking, classic results under a real AI-off notice.
