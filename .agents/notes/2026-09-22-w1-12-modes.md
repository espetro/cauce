# W1-12: feature gates + CURRENT_WAVE blocker

- Cargo features live on `oxe-server` and are forwarded by `oxe-cli`
  (`oxe-server` dep is `default-features = false`). Defaults `ui mcp ai`;
  `otlp` non-default. `RouteSpec::requires` filters via
  `oxe_server::feature_enabled` (compile time) AND `RouterOptions.ui`
  (runtime `--headless`); unknown names fail closed.
- `CURRENT_WAVE` stayed 0: `GET /metrics` (W1-09) had no handler arm when
  this landed. Once `/metrics` has an arm, bump `CURRENT_WAVE` to 1 — the
  must-implement assert in `build_router_opts` then covers all wave-1 rows.
- New feature-gated modules need: feature on oxe-server + forward on
  oxe-cli, `#[cfg]` on the module + its `handler_for` arm(s), `#[cfg]` on
  the test file(s), and the `requires` name added to `feature_enabled`.
- `mise run check:features` compiles `-p oxe-cli --all-targets
  --no-default-features --features mcp`; keep e2e test code cfg-clean under
  that set (rmcp dev-deps stay unconditional so gated test files still
  compile).
