# UI loop run 1 ledger (2026-09-18)

Scope: the 9 built screen states (landing, search classic results/zero/error, AI unavailable/stream-failed/empty-sources, history, dashboard) at 3 viewports. Test backend: Wikipedia OpenSearch (title-prefix search, no snippets). Evaluator: opus, isolated, fresh per iteration.

Outcome: FAIL after 3 iterations (cap reached). Hard gates green on 27 of 27 cells in the final capture, e2e 6 passed and 14 skipped (unbuilt checkpoints), vitest 63, pytest 148.

## Ledger

| Iteration | Closed | Opened |
|-----------|--------|--------|
| 1 (mechanical) | none | unnamed buttons, mobile overflow, contrast on error and dashboard text, console error from a rejected loader, landing pill overflow |
| 2 | all five families above | 9: results header not the shared pill with the Search/AI toggle (P0), AI-unavailable dropped the classic results (P0), empty-sources copy and escape hatch missing (P0), mid-stream failure lost the partial answer (P0), no result snippet or cached preview (P0), landing pill at 63% of viewport height (P1), results column flush left vs centered (P1), cache badge without age (P1), no h1 on /search (P2), no `ask AI instead` on zero and error rows (P2) |
| 3 | shared pill with mode toggle, AI-unavailable classic results, empty-sources copy, partial answer kept, column origin, h1, ask-AI hatches, mobile hero anchoring | 7 (below) |

## Unresolved after iteration 3

- P0 result snippet and `cached page text preview` missing. Snippets: the UI renders `content` when present; the Wikipedia OpenSearch backend returns none, so this is test-data gated. Preview: needs cached page text on the wire, not built. Never marked fixed.
- P1 cache badge shows no age: `/search` JSON carries no cache age. Backend change needed. Never marked fixed.
- P1 result title links underlined at rest (reference: no underline until hover).
- P1 dashboard cache panels show placeholders instead of `/api/stats` cache fields (dashboard.md).
- P2 zero and empty-source states carry a glyph and the AI empty state keeps `confidence: 0%` meta (checkpoint 14 asserts that meta, so removing it needs a checkpoint decision).
- P2 header: no active `[search]` marking and no version label.
- P2 desktop hero pill at 40% of viewport height, just under the 42-45% band (mobile 45%). Overshoot from the fix for the 63% finding.

## Other things the run found and fixed

- `useAnswerStream` had no dependency array or cleanup, so it re-POSTed `/answer` on every render and a repeat overwrote the kept partial text. Now keyed on `[query, force, dispatch]` with abort on cleanup.
- `force=error` fixture now streams a partial delta before the error frame, so checkpoint 13 exercises "partial text kept".
- Checkpoint 3 screenshot tolerance removed after it hid a stale baseline.

## Not done

- Spec write-back (UILOOP section 8) is not done because no iteration passed. Baselines in `tests/e2e/checkpoints.md.ts-snapshots/` were regenerated only so the e2e suite reflects the new pill and column.
- Not assessable from static evidence: interaction states, motion, ARIA, type metrics, a completed AI answer with source cards (needs a configured model).
- Real AI-unavailable (no `force` fixture) has no classic results under the notice, because the loader skips search in AI mode unless `force=ai-off`.
